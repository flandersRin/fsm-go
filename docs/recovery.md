# 恢复机制

workflow-go 的恢复能力来自持久化的流程快照、任务记录和执行历史。

## 重试

任务失败后，如果还没有达到最大尝试次数，运行时会把任务标记为 `retrying`，并设置下一次执行时间。

## 超时

任务定义可以设置超时时间。运行时执行任务时会根据超时控制上下文。

## 补偿

任务可以声明补偿任务。当任务最终失败后，运行时会调度补偿任务，并把补偿写入时间线。

## 进程重启

进程重启后，新的 worker 调用 `RunDueTasks`，即可继续扫描未完成任务并恢复执行。
